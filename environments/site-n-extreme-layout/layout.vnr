theme "Extreme Layout Neon" {
  variables {
    --spm-bg-primary: "#090a0f";
    --spm-bg-surface: "#12151e";
    --spm-bg-element: "#1c202e";
    --spm-text-primary: "#ffffff";
    --spm-text-muted: "#a0a5b5";
    --spm-accent: "#ff0055";
    --spm-border-contrast: "rgba(255, 0, 85, 0.2)";
    --spm-radius: "12px";
  }

  customStyles {
    "#inline-stress-container { width: 100% !important; margin: 0 !important; float: none !important; }"
    ":host { display: block; width: 100%; box-sizing: border-box; }"
  }
}

class MediaCardBase {
  bind title: "h3 | text";
  bind imageUrl: "img | attr:src";
  bind imageSrcset: "img | attr:srcset";
}

class MediaCardDetailed extends MediaCardBase {
  bind rating: "span | text | cleanNumber";
}

// Reconstruct Section 1 Inline Stress Container
reconstruct "#section-1" -> UiModernGridPage {
  pageTitle: "Extreme Layout & Shadow DOM Gallery";
  subtitle: "Stress testing inline CSS overrides, dynamic DOM mutations, and Shadow DOM bounds";
  columns: 3;
  sticky: true;

  child galleryItems extends MediaCardDetailed {
    selector: "#inline-stress-container";
  }

  preserve {
    shadowDomHost: "#shadow-host";
  }
}

// Replace Legacy Header with Modern React Header
selector "header" -> UiNavHeader {
  action: replace;
  siteName: "Layout Sandbox";
  sticky: true;
  primaryLinks: [
    { "label": "Gallery", "url": "/gallery" },
    { "label": "Mutations", "url": "/mutations" },
    { "label": "Shadow DOM", "url": "/shadow" }
  ];
}

// Hide Element with Negative Margins and Bad Z-Index
selector "#negative-margin-container" {
  action: hide;
}
