# QA Environment: Site E - User Account Dashboard (`site-e-dashboard`)

This environment tests `UiDashboardPage` and `UiToastContainer` components.

## Focus Areas
- Action cards rendering and URL navigation labels.
- Hidden security input preservation (`form | hiddenInputs`).
- Viewport scaling and toast notification overlays.
