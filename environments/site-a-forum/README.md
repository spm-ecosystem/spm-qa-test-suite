# QA Environment: Site A - Legacy Forum

This environment tests the `UiCommentListPage` component against legacy discussion threads.

## Focus Areas
- HTML pipe safety in comment body rendering.
- Null fallback when user avatars are missing.
- Responsive layout under narrow viewports (< 375px).
