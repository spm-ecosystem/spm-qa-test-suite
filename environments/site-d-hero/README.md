# QA Environment: Site D - Landing Portal (`site-d-hero`)

This environment tests `UiHeroLanding`, `UiSearchBar`, and `UiTagBadge` components.

## Focus Areas
- Full viewport landing hero banner rendering.
- Search form submission handling inside hero headers.
- Fallback layout when logo image `logoUrl` is missing or fails to load.
